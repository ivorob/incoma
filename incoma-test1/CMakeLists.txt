CMAKE_MINIMUM_REQUIRED (VERSION 3.0)
PROJECT (incoma-test1)

SET (SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})
SET (SOURCES 
        ${SRC_DIR}/main.cpp
    )

MESSAGE (${SOURCES})
ADD_EXECUTABLE (incoma-test1 ${SOURCES})
